# Generated by tools/openapicheck -write-manifest. Do not edit by hand.
#
# This is the flat list of operations the OpenAPI document promises to serve. CI
# regenerates it and fails if it differs, so the documented surface and the
# checked-in manifest can never drift apart. The check is one-directional on
# purpose: when internal/server/server.go is restored (issue #186) the manifest
# becomes a comparison between the router's registered routes and the document,
# and this file is the expected output of that comparison either way.

routes:
  - "DELETE /v1/keys/{id}"
  - "DELETE /v1/schedules/{id}"
  - "DELETE /v1/webhooks/{id}"
  - GET /health
  - GET /metrics
  - GET /v1/admin/compliance/reviews
  - "GET /v1/admin/compliance/reviews/{id}"
  - GET /v1/admin/compliance/sanctions-status
  - GET /v1/admin/fees/collected
  - GET /v1/admin/reconciliation/summary
  - GET /v1/claimable-balances
  - "GET /v1/claimable-balances/{id}"
  - GET /v1/fees
  - GET /v1/fx/rates
  - GET /v1/keys
  - GET /v1/schedules
  - GET /v1/transfers
  - "GET /v1/transfers/batch/{batchId}"
  - "GET /v1/transfers/batch/{batchId}/export"
  - "GET /v1/transfers/{id}"
  - GET /v1/usage
  - "GET /v1/wallets/{id}"
  - "GET /v1/wallets/{id}/balances"
  - GET /v1/webhooks
  - "GET /v1/webhooks/{id}/deliveries"
  - "PATCH /v1/schedules/{id}"
  - "POST /v1/admin/compliance/reviews/{id}/approve"
  - "POST /v1/admin/compliance/reviews/{id}/reject"
  - POST /v1/admin/reconciliation/run
  - POST /v1/auth/login
  - POST /v1/auth/register
  - POST /v1/claimable-balances
  - "POST /v1/claimable-balances/{id}/claim"
  - POST /v1/fx/convert
  - POST /v1/fx/quote
  - POST /v1/keys
  - POST /v1/schedules
  - POST /v1/transfers
  - POST /v1/transfers/batch
  - POST /v1/wallets
  - "POST /v1/wallets/{id}/deposit/fiat"
  - "POST /v1/wallets/{id}/trustlines"
  - "POST /v1/wallets/{id}/withdraw/fiat"
  - POST /v1/webhooks
  - "POST /v1/webhooks/fiat/{provider}"
